/**
 * @param {string} input
 * @returns {?string}
 */
export const parse = (input) => (input === "" ? null : input)

/**
 * @param {string} input
 * @returns {string}
 */
export const format = (input) => String(input)
